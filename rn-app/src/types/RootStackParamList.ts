type RootStackParamList = {
  profileOnboard: {
    accountData: { user_id: string; email_address: string };
  };
  interestOnboard: {
    accountData: { user_id: string; email_address: string };
  };
};
